---
title: GL Accounts
description: A chart of accounts semantic contract, defining financial account hierarchy used for reporting and metrics.
---

export const caption = "A list of GL Accounts"
export const caption_segmented = "A list of GL Accounts segmented with business units"
export const table_name="gl_accounts"

## Semantic Contract
### Definitions
| Column | Description |
|--------|-------------|
| id | Unique identifier for the GL account. |
| name | Name of the GL account (e.g., Revenue, Cost of Goods Sold, Marketing Expense). |
| parent_gl | Self-reference to the GL account for hierarchy (nullable if top-level). |
| gl_account_type_id | Reference to the [GL Account Type](./gl-accounts-types). |
| business_unit_id | Reference to the business unit (only if segmented; links to [Business Units](./business-units)). |

### Schema & Sample

<Callout>
    The GL Accounts IDs `3000`, `5000`, `7000`, `8000` are reserved for `gross_margin`, `contribution_margin`, `total_cost`, and `ebitda`. If you need to repurpose these IDs, make sure to adjust the rlated transformations accordingly: [PnL Metrics](transformations/pnl-metrics)
</Callout>

<Callout>
    We favor an opinionated numbering scheme that follows a parent > children structure. For example, revenue is defined as `1000`, and their children `1010`, `1020`, and `1030`. This allows for better organisation and displaying later in reports.
</Callout>

<GLAccountsSelector
  segmented={
  <RenderTable csvPath={`registry/semantic_layers/${table_name}_segmented.csv`} caption={caption_segmented}>
  <ReadComponents filePath={`registry/semantic_layers/sqls/${table_name}_segmented.sql`}/>
  </RenderTable>
  }
  simple={
  <RenderTable csvPath={`registry/semantic_layers/${table_name}_simple.csv`} caption={caption}>
  <ReadComponents filePath={`registry/semantic_layers/sqls/${table_name}_simple.sql`}/>
  </RenderTable>
  }
/>

## Data Format

<GLAccountsSelector
  segmented={
    <TypeTableFromFile filePath={`registry/semantic_layers/${table_name}_segmented.json`} />
  }
  simple={
    <TypeTableFromFile filePath={`registry/semantic_layers/${table_name}_simple.json`} />
  }
/>

## Dependencies

- **[GL Accounts Types](./gl-accounts-types)**
- **[Business Units](./business-units)**: required when segmented.
